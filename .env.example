# AI StoryBound 环境变量示例
# 复制为 .env 并按需修改。部署到 Cloudflare 时在 wrangler/dashboard 配置 secrets。

# 本地开发时 @nuxthub/core 默认用本地 SQLite 文件模拟 D1、本地 blob 目录模拟 R2,无需任何配置即可跑通。
# 部署 Cloudflare 时用 wrangler CLI / dashboard 创建真实的 D1 数据库、R2 bucket 并写入 binding。

# LLM 网关(尚未实现,占位)
# LLM_API_KEY=
# LLM_BASE_URL=
# LLM_MODEL=
