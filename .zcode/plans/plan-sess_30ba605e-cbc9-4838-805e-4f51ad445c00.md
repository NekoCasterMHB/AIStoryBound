把角色卡「属性(键:值)」从行内直改改为「卡片预览 + 模态框编辑」,并统一“添加已知字段/自定义键”都走模态。

改动仅 app/components/CharacterCardsModal.vue:
1. script:新增 editAttrOpen 与 editAttr 状态({mode add|edit, rowIndex, scope desc|free, kind, key, text});新增 openAddFree/openAddKnown(键预填锁死,desc 注册类型)/openEditRow(desc 行键名只读、free 可改)/saveEditAttr(内容为空视为删除并移除卡片;重名拦截;desc tags 内容按换行/顿号/逗号拆数组,其余按 desc 注册形态或 free 类型 freeValueOf 解析;保存后 commitAttr);新增 attrPreview 供卡片预览。
2. template:「属性(键:值)」区行内编辑器移除,改卡片列表:键名徽标 + 内容预览(自动换行两行截断,空显示灰字提示)+ 编辑按钮 + 删除按钮;「添加已知字段」下拉与「自定义键」改为打开模态;新增内嵌 UModal(属性名输入:desc 只读、free 可编辑;类型选择仅 free 显示默认文本;内容 UTextarea autoresize 自动高度 w-full 占满,placeholder 按形态给提示;取消/保存)。
3. 不动:分段剧情/状态折叠栏、B 类结构化控件、commitAttr 存储口径、解释器/落库。

验证:vue-tsc + eslint;浏览器实测 book2 作品增删改属性→保存→重开回读、属性展示正常。