## 计划:个人中心页去掉「返回首页」和「退出」按钮

**改动文件**:`app/pages/profile.vue`(仅此一个文件)

1. **删除页面头部右侧按钮组**:移除「返回首页」(`to="/"`)和「退出」(`@click="onLogout"`)两个 UButton 及其外层 `flex gap-2` 容器。头部保留「个人中心」标题和用户信息,布局不变。

2. **删除脚本中不再使用的 `onLogout` 函数**(现在变成死代码)。

**不改动其他内容**:顶部导航栏(布局 `default.vue`)已有「退出登录」用户菜单和点击 logo 返回首页的入口,无需新增或移动任何按钮;`authClient.signOut()` 功能不受影响。