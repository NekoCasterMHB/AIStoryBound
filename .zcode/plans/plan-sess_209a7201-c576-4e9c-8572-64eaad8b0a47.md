## 仅写入一份文档:docs/format-v2.md

把本会话锁定的 v2 设计固化为 `docs/format-v2.md`(不改任何运行时代码)。内容即上面已展开的完整设计,包含:

1. **背景与动机**:schema 僵化导致异版本 zip 崩溃的事故与结论
2. **v2 容器规范(aisb-book)**:manifest.json + fulltext.txt + plot/NNN.json + characters/姓名.json,附 manifest 字段
3. **plot 段 JSON 契约**:index/title/cast/beat/place/turn/hook/人物状态/text(内联切片),含设计取舍
4. **characters JSON 契约**:必生成中文保留键(姓名/角色/身份/外貌/性格/背景/目标/关系)、可选保留键、自由区;值容忍规则;保留键宽度表
5. **按段演化**:状态放 plot 段内(替代 chapterVariants)
6. **动态渲染与编辑规格**:自由键名、显式类型选择、卡片折叠/编辑展开、外来结构通用渲染
7. **保留键解释器与引擎读取层**:中文键 ↔ 引擎语义字段映射表 + 归一规则 + profile 补充设定
8. **转换器与兼容策略**:aisb-share v1 ↔ v2 双向转换,旧 zip 永久可导入,不强制迁移
9. **分阶段实施计划 P0-P6**(仅记录计划)
10. **风险与开放问题**(配角弧线挂载、D1 预置/工坊、阅读器组装、体积上限、保留键覆盖规则、主角锚演进)

完成后不做任何代码改动,等你评审文档。